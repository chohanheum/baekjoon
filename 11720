#include <stdio.h>
#pragma warning(disable:4996)

int main()
{
    int num;
    int sum = 0;
    char arr[100];

    scanf("%d", &num);
    scanf("%s", arr);

    for (int i = 0; i < num; i++)
    {
        sum += arr[i] - '0';
    }
         

    printf("%d", sum);

    return 0;
}
